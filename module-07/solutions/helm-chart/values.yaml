# Generated by: make helm-chart
# This is what the Makefile helm-chart target produces
#
# Note: These values are informational. The actual manifests are pre-rendered
# from kustomize and stored in templates/manifests.yaml. To use these values,
# you would need to template the manifests.yaml file.

image:
  repository: ghcr.io/example/postgres-operator
  tag: 0.1.0
  pullPolicy: IfNotPresent

replicaCount: 1

resources:
  limits:
    cpu: 500m
    memory: 128Mi
  requests:
    cpu: 10m
    memory: 64Mi

leaderElection:
  enabled: false

namespace: postgres-operator-system
